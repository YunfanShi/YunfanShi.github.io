# 已泄露扩展密钥的历史处置

当前工作树已完成：

- manifest 中的私钥已删除。
- 被忽略的本地 `companion-extension.pem` 已删除。
- 旧 CRX/ZIP 已删除。
- 当前扫描器不会在工作树和扩展包中接受 PEM 私钥。

仍需仓库负责人批准的动作：

- Git 历史提交 `afa0786 fix: 固定扩展 ID` 仍包含旧私钥内容。
- 需要先吊销旧密钥，再决定是否使用 `git filter-repo` 或等效工具改写所有本地/远程引用。
- 历史改写会改变提交哈希并要求远程强制推送，不能由本轮自动执行。

在历史处置完成前，旧私钥按永久泄露处理，不能用于任何扩展签名、OAuth 或部署。
