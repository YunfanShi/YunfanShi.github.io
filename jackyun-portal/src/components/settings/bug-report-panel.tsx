'use client';

import { useEffect, useState } from 'react';
import { getMyBugReports, submitBugReport, type UserBugReport } from '@/actions/feedback';
import logger from '@/lib/logger';

const statusName: Record<string, string> = { open: '已提交', in_progress: '处理中', resolved: '已解决', closed: '已关闭' };
const statusStyle: Record<string, string> = { open: 'bg-blue-50 text-[#175cd3]', in_progress: 'bg-[#fffaeb] text-[#b54708]', resolved: 'bg-[#ecfdf3] text-[#027a48]', closed: 'bg-slate-100 text-[#475467]' };

export default function BugReportPanel() {
  const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [severity, setSeverity] = useState<'low' | 'normal' | 'high' | 'critical'>('normal'); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); const [reports, setReports] = useState<UserBugReport[]>([]);
  const refresh = () => getMyBugReports().then(setReports).catch(() => {});
  useEffect(() => { refresh(); }, []);
  const send = async () => { setLoading(true); setMessage(''); const result = await submitBugReport({ title, description, severity, pageUrl: location.href, diagnostics: logger.getDiagnosticSnapshot() }); setMessage(result.success ? result.mailSent ? '反馈和脱敏诊断日志已提交，管理员已收到邮件提醒。' : `反馈与诊断日志已保存；${result.mailError ?? '管理员邮件提醒未发送'}。` : result.error ?? '提交失败'); if (result.success) { setTitle(''); setDescription(''); refresh(); } setLoading(false); };
  return <section className="rounded-[12px] border border-[var(--card-border)] bg-[var(--card)] p-5"><div className="mb-4 flex items-center gap-2"><span className="material-icons-round text-[#EA4335]">bug_report</span><h2 className="text-sm font-semibold uppercase tracking-wider text-[var(--muted-foreground)]">Bug 反馈</h2></div><p className="mb-3 text-xs text-[var(--muted-foreground)]">提交时会附上已脱敏的网络、错误与操作轨迹，帮助管理员复现问题。</p><div className="space-y-3"><input value={title} onChange={(event) => setTitle(event.target.value)} placeholder="问题标题" className="w-full rounded-lg border border-[var(--card-border)] bg-[var(--background)] px-3 py-2 text-sm" /><textarea value={description} onChange={(event) => setDescription(event.target.value)} placeholder="请描述复现步骤、预期结果和实际结果" rows={4} className="w-full rounded-lg border border-[var(--card-border)] bg-[var(--background)] px-3 py-2 text-sm" /><select value={severity} onChange={(event) => setSeverity(event.target.value as typeof severity)} className="rounded-lg border border-[var(--card-border)] bg-[var(--background)] px-3 py-2 text-sm"><option value="low">轻微</option><option value="normal">普通</option><option value="high">严重</option><option value="critical">紧急</option></select><button type="button" disabled={loading || title.trim().length < 3 || !description.trim()} onClick={send} className="ml-2 rounded-lg bg-[#EA4335] px-4 py-2 text-sm font-medium text-white disabled:cursor-not-allowed disabled:opacity-50">{loading ? '提交中…' : '提交反馈'}</button>{message && <p className="text-sm text-[var(--muted-foreground)]">{message}</p>}</div><div className="mt-6 border-t border-[var(--card-border)] pt-4"><div className="flex items-center justify-between"><h3 className="text-sm font-semibold text-[var(--foreground)]">我的反馈</h3><button type="button" onClick={refresh} className="text-xs font-medium text-[#155eef]">刷新</button></div>{reports.length ? <div className="mt-3 divide-y divide-[var(--card-border)]">{reports.map((report) => <article key={report.id} className="py-3"><div className="flex items-start gap-2"><p className="min-w-0 flex-1 truncate text-sm font-medium text-[var(--foreground)]">{report.title}</p><span className={`shrink-0 rounded-full px-2 py-0.5 text-[10px] font-medium ${statusStyle[report.status]}`}>{statusName[report.status]}</span></div><p className="mt-1 text-xs text-[var(--muted-foreground)]">最近更新：{new Date(report.updated_at).toLocaleString('zh-CN')}</p></article>)}</div> : <p className="mt-3 text-xs text-[var(--muted-foreground)]">尚未提交反馈。</p>}</div></section>;
}
